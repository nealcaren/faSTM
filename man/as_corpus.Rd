% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/corpus.R
\name{as_corpus}
\alias{as_corpus}
\title{Build a faSTM corpus from prepared text}
\usage{
as_corpus(x, meta = NULL, ...)
}
\arguments{
\item{x}{A \code{quanteda} \code{dfm}, a document-term \code{Matrix}/matrix (documents in
rows, terms in columns, with \code{colnames}), or an existing \code{faSTM_corpus}.
For a tidy (long) term table use \code{\link[=from_tidy]{from_tidy()}}.}

\item{meta}{Optional data.frame of document metadata, one row per document,
aligned to \code{x}. For a \code{dfm}, defaults to \code{quanteda::docvars(x)}.}

\item{...}{Unused.}
}
\value{
A \code{faSTM_corpus}: a list with \code{documents} (named list of 2×n integer
matrices: row 1 = 1-based term id, row 2 = count), \code{vocab} (character),
\code{meta} (data.frame or NULL), and \code{word_counts} (corpus term frequencies).
}
\description{
faSTM does not do its own tokenization — it reads an already-prepared
document-term representation from the tools the field already uses
(\code{quanteda}, \code{tidytext}) or a plain sparse matrix. \code{as_corpus()} normalizes
any of these into the structure \code{\link[=stm]{stm()}} consumes, dropping empty documents
and re-indexing the vocabulary, with metadata kept aligned.
}
