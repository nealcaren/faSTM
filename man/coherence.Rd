% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inspect.R
\name{coherence}
\alias{coherence}
\title{Topic coherence (Mimno / NPMI / c_v)}
\usage{
coherence(model, measure = c("mimno", "npmi", "c_v"), M = 10L)
}
\arguments{
\item{model}{A faSTM fit (carries its DTM).}

\item{measure}{\code{"mimno"}, \code{"npmi"}, or \code{"c_v"}.}

\item{M}{Top words per topic.}
}
\value{
A numeric vector, one coherence score per topic.
}
\description{
Coherence scores for each topic's top-\code{M} words, computed from the fit's
stored document-term matrix. \code{"mimno"} is the UMass-style score of
\code{\link[=semantic_coherence]{semantic_coherence()}}; \code{"npmi"} averages pairwise normalized PMI; \code{"c_v"} is
the Roeder et al. (2015) measure (one-set segmentation, NPMI confirmation,
cosine aggregation). NPMI/c_v use \emph{document} co-occurrence as the probability
estimator. Higher is more coherent (npmi/c_v are roughly in \link{-1, 1}).
}
