% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/content-trajectory.R
\name{content_trajectory}
\alias{content_trajectory}
\title{Group-contrast content trajectory over an ordered content covariate}
\usage{
content_trajectory(
  object,
  words,
  groups = NULL,
  topic = NULL,
  anchor_words = NULL,
  ci = FALSE,
  corpus = NULL,
  fit_args = NULL,
  B = 50L,
  level = 0.95,
  seed = NULL
)
}
\arguments{
\item{object}{A fitted \code{STM}/\code{faSTM} object with a \code{content_time} covariate.}

\item{words}{Character vector of words to trace.}

\item{groups}{Length-2 character vector \code{c(g1, g2)} of base content-group levels;
the contrast is \code{p(word | g1) - p(word | g2)}. Defaults to the first two base
groups in the fit.}

\item{topic}{Integer topic index in \code{object}'s numbering (the point-estimate
topic). Ignored when \code{anchor_words} is supplied.}

\item{anchor_words}{Character vector identifying the target topic by top-word
overlap. Required when \code{ci = TRUE} (to realign topics across bootstrap refits);
optional otherwise. If given, it overrides \code{topic}.}

\item{ci}{Logical; if \code{TRUE}, attach bootstrap confidence intervals.}

\item{corpus}{The \code{faSTM_corpus} the model was fit on (required when \code{ci = TRUE}).}

\item{fit_args}{Named list of arguments passed to \code{\link[=stm]{stm()}} to refit each bootstrap
replicate, e.g. \code{list(K = 12, prevalence = ~ party + s(congress), content = ~ party, content_time = ~ congress, content_prior = "l1")}. Required
when \code{ci = TRUE}.}

\item{B}{Integer number of bootstrap replicates (default 50).}

\item{level}{Confidence level for the percentile interval (default 0.95).}

\item{seed}{Optional integer for reproducible resampling.}
}
\value{
A data frame with columns \code{word}, \code{period}, and \code{estimate}, and, when
\code{ci = TRUE}, \code{conf.low} and \code{conf.high}.
}
\description{
For a model fit with a \code{content_time} covariate (see \code{\link[=stm]{stm()}}), trace how the
difference in a word's probability between two content groups moves across the
ordered periods. This is the quantity the smoothed content-time covariate makes
estimable: whether and how two groups' \emph{wording} of a topic diverges over time.
}
\details{
With \code{ci = TRUE} the function attaches a nonparametric document bootstrap
confidence interval: it resamples documents with replacement, refits the model
\code{B} times (using \code{fit_args} on \code{corpus}), and takes percentile intervals of the
contrast at each period. Because each bootstrap fit numbers its topics
differently, the target topic is realigned in every replicate by overlap with
\code{anchor_words}, so \code{anchor_words} is required when \code{ci = TRUE}.

The bootstrap refits the full model \code{B} times and is therefore expensive; it is
opt-in, and \code{B} should be chosen accordingly. The intervals are wider where the
data are thin (typically the first and last periods, where a first-order random
walk is least constrained), which is the honest signal a point trajectory hides.
}
\examples{
\donttest{
data(congress)
fit <- stm(congress, K = 12, prevalence = ~ party + s(congress),
           content = ~ party, content_time = ~ congress, content_prior = "l1")
econ <- c("tax", "budget", "deficit", "jobs", "spending", "economy")
# point estimate
content_trajectory(fit, words = c("tax", "wage"),
                   groups = c("Democrat", "Republican"), anchor_words = econ)
# with a (small, illustrative) bootstrap interval
content_trajectory(fit, words = c("tax", "wage"),
                   groups = c("Democrat", "Republican"), anchor_words = econ,
                   ci = TRUE, corpus = congress, B = 20, seed = 1,
                   fit_args = list(K = 12, prevalence = ~ party + s(congress),
                                   content = ~ party, content_time = ~ congress,
                                   content_prior = "l1"))
}
}
