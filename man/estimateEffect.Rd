% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate-effect.R
\name{estimateEffect}
\alias{estimateEffect}
\title{Estimate covariate effects on topic prevalence (method of composition)}
\usage{
estimateEffect(
  formula,
  stmobj,
  metadata = meta,
  uncertainty = c("Global", "None", "Local"),
  nsims = 100L,
  seed = NULL,
  meta = NULL,
  documents = NULL,
  combine = NULL,
  weights = NULL,
  cluster = NULL,
  ...
)
}
\arguments{
\item{formula}{A formula whose LHS lists topic numbers (e.g. \code{1:5 ~ treatment})
or whose LHS is empty to use all topics; RHS gives the covariates. Random-
effect terms \code{(term | group)} are supported (fits \code{lme4::lmer} per draw and
pools the fixed effects; variance components are stored).}

\item{stmobj}{A faSTM fit (from \code{\link[=stm]{stm()}}).}

\item{metadata}{A data.frame of covariates aligned to the documents.}

\item{uncertainty}{\code{"Global"} (method of composition over posterior draws,
default) or \code{"None"} (single OLS on the posterior-mean theta).}

\item{nsims}{Posterior draws for \code{uncertainty = "Global"}.}

\item{seed}{Optional seed for the posterior draws.}

\item{documents}{Accepted for stm compatibility (faSTM reads nu from the fit).}

\item{combine}{Optional list of topic vectors to also estimate as aggregate
topics (each set's proportions are summed before regressing); named entries
set the coefficient names. E.g. \code{combine = list(econ = c(3, 7))}.}

\item{weights}{Optional per-document survey/sampling weights (weighted OLS).}

\item{cluster}{Optional per-document cluster ids for cluster-robust SEs.}

\item{...}{Unused (stm signature compatibility).}
}
\value{
An object of class \code{c("faSTM_effect", "estimateEffect")} with a
\code{summary()} method, holding pooled coefficients and standard errors per
topic.
}
\description{
A drop-in for \code{\link[stm:estimateEffect]{stm::estimateEffect()}} that propagates per-document
topic-estimation uncertainty honestly: it regresses each posterior draw of
topic proportions on the covariates and pools the per-draw fits by Rubin's
rules. This is topica's "honest" effect estimator, the reason faSTM ships its
own rather than inheriting stm's.
}
