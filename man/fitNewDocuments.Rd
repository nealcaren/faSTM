% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{fitNewDocuments}
\alias{fitNewDocuments}
\title{Infer topics for new documents (stm-compatible signature)}
\usage{
fitNewDocuments(
  model,
  documents,
  newData = NULL,
  origData = NULL,
  prevalence = NULL,
  betaIndex = NULL,
  prevalencePrior = c("Average", "Covariate", "None"),
  contentPrior = c("Covariate", "Average"),
  returnPosterior = FALSE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{model}{A faSTM fit.}

\item{documents}{New documents: a \code{faSTM_corpus}/\code{dfm}/matrix (aligned to the
model vocabulary), or an stm-style list of 2 x n integer matrices indexed
into \code{model$vocab}.}

\item{newData, origData}{Covariate frames for the new and original documents
(used by \code{prevalencePrior = "Covariate"} to set each document's prior mean).}

\item{prevalence}{Prevalence formula (same RHS as the fit) for the covariate prior.}

\item{betaIndex}{Integer per-document content-group index (content models).}

\item{prevalencePrior}{\code{"Average"} (global prior mean, default) or \code{"Covariate"}
(per-document mean from \code{prevalence}/\code{newData}).}

\item{contentPrior}{\code{"Covariate"} (use the group's topic-word matrix via
\code{betaIndex}, default) or \code{"Average"} (group-marginal).}

\item{returnPosterior}{If \code{TRUE}, return \code{list(theta, eta, nu)} (per-document
variational mean and Laplace covariance); otherwise a documents x K theta matrix.}

\item{verbose}{Logical.}

\item{...}{Ignored (stm signature compatibility).}
}
\value{
A theta matrix, or a posterior list when \code{returnPosterior = TRUE}.
}
\description{
Drop-in for \code{\link[stm:fitNewDocuments]{stm::fitNewDocuments()}}. Holds the fitted topics fixed and runs
the variational E-step for each new document. Supports stm's prior modes and
posterior return.
}
