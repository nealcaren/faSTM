% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{fit_stm}
\alias{fit_stm}
\title{Fit a structural topic model and return its raw arrays.}
\usage{
fit_stm(
  docs_flat,
  doc_lens,
  num_types,
  num_topics,
  em_iters,
  em_tol,
  sigma_shrink,
  prevalence,
  num_features,
  content_groups,
  num_groups,
  init_spectral,
  init_beta,
  gamma_l1_alpha,
  diagonal,
  seed,
  inference,
  batch_size,
  tau,
  kappa,
  num_threads
)
}
\description{
Inputs are pre-converted in \code{R/stm.R}:
\itemize{
\item \code{docs_flat} / \code{doc_lens}: documents as one concatenated 0-based token-id
stream plus per-document lengths (counts already expanded). Reassembled
here into \verb{Vec<Vec<u32>>}, the shape \code{fit_ctm} wants.
\item \code{prevalence}: row-major D×P design matrix flattened (NULL if none).
\item \code{content_groups}: per-doc 0-based group id (NULL if none); \code{num_groups}.
}
}
\details{
\code{inference}: "batch" -> \code{fit_ctm} (parity-validated). "svi" -> \code{fit_ctm_svi}
once topica #231 PR B (STM-SVI) is in the pinned revision; the R layer gates
the prevalence/content + svi combination until then.
}
