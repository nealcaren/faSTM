% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{infer_theta_new}
\alias{infer_theta_new}
\title{Out-of-sample topic inference: for each new document, run the variational
E-step against fixed globals (β, μ, Σ⁻¹) and return θ. Documents are passed
sparse — \code{words} are 0-based ids into the \emph{fitted model's} vocabulary
(out-of-vocabulary terms dropped by the R caller) with their \code{counts},
concatenated, plus per-document term counts \code{doc_nterms}.}
\usage{
infer_theta_new(
  beta_flat,
  num_topics,
  num_types,
  mu,
  siginv,
  words,
  counts,
  doc_nterms
)
}
\description{
Out-of-sample topic inference: for each new document, run the variational
E-step against fixed globals (β, μ, Σ⁻¹) and return θ. Documents are passed
sparse — \code{words} are 0-based ids into the \emph{fitted model's} vocabulary
(out-of-vocabulary terms dropped by the R caller) with their \code{counts},
concatenated, plus per-document term counts \code{doc_nterms}.
}
