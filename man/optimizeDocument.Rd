% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stm-helpers.R
\name{optimizeDocument}
\alias{optimizeDocument}
\title{Per-document variational E-step (stm-compatible)}
\usage{
optimizeDocument(document, eta, mu, beta, sigma = NULL, sigmainv = NULL, ...)
}
\arguments{
\item{document}{A 2 x n integer matrix (1-based vocab ids; counts).}

\item{eta}{Ignored starting value (kept for signature compatibility).}

\item{mu}{Prior mean (length K-1).}

\item{beta}{K x V topic-word probability matrix.}

\item{sigma, sigmainv}{Prior covariance or its inverse (supply one).}

\item{...}{Ignored (stm signature compatibility).}
}
\value{
A list with \code{lambda}, \code{nu}, and \code{theta}.
}
\description{
Port of \code{stm:::optimizeDocument}'s interface: infers one document's topic
proportions against fixed globals and returns its variational mean \code{lambda}
(eta), Laplace covariance \code{nu}, and \code{theta}.
}
