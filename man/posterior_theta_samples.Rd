% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior.R
\name{posterior_theta_samples}
\alias{posterior_theta_samples}
\title{Draw from the per-document topic-proportion posterior}
\usage{
posterior_theta_samples(model, nsims = 100L, seed = NULL)
}
\arguments{
\item{model}{A faSTM fit (from \code{\link[=stm]{stm()}}).}

\item{nsims}{Number of posterior draws.}

\item{seed}{Optional integer seed for reproducible draws.}
}
\value{
A \code{nsims}-length list of \verb{D x K} theta matrices.
}
\description{
The variational (Laplace) posterior of each document's logit-topic vector is
\code{eta_d ~ N(lambda_d, nu_d)}, both stored on a faSTM fit. This draws \code{nsims}
samples of theta per document by sampling eta and applying the softmax (with
the reference topic appended as 0). This is the pure-R equivalent of topica's
\code{posterior_theta_samples}; no Rust call is needed because \code{eta} + \code{nu} fully
describe the posterior. Feeds \code{\link[=estimateEffect]{estimateEffect()}}'s method of composition.
}
