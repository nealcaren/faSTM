% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conveniences.R
\name{s}
\alias{s}
\title{Spline term for prevalence formulas}
\usage{
s(x, df, ...)
}
\arguments{
\item{x}{Numeric predictor.}

\item{df}{Basis dimension; defaults to \code{min(10, length(unique(x)) - 1)}.}

\item{...}{Passed to \code{\link[splines:bs]{splines::bs()}}.}
}
\value{
A spline basis matrix (with class \code{"s"}).
}
\description{
A b-spline basis for smooth covariate effects, e.g. \code{prevalence = ~ s(day)}.
Matches \code{stm::s()} exactly — including the \code{df = min(10, nval - 1)} default —
so spline-term coefficients agree with \code{stm}. (You can also use
\code{\link[splines:bs]{splines::bs()}}/\code{\link[splines:ns]{splines::ns()}} directly.)
}
