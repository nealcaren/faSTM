% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search-k.R
\name{search_k}
\alias{search_k}
\title{Search over the number of topics K}
\usage{
search_k(
  corpus,
  K,
  prevalence = NULL,
  content = NULL,
  heldout = TRUE,
  proportion = 0.5,
  residuals = FALSE,
  cores = 1L,
  M = 10L,
  seed = 1L,
  measure = c("mimno", "npmi", "c_v"),
  verbose = FALSE,
  ...
)
}
\arguments{
\item{corpus}{A \code{faSTM_corpus} (from \code{\link[=as_corpus]{as_corpus()}}).}

\item{K}{Integer vector of topic counts to try.}

\item{prevalence, content}{Optional covariate formulas (see \code{\link[=stm]{stm()}}).}

\item{heldout}{Logical; compute held-out likelihood via document completion.}

\item{proportion}{Held-out token fraction (passed to \code{\link[=make_heldout]{make_heldout()}}).}

\item{cores}{Number of K-fits to run in parallel (forked; 1 = sequential).
When \code{cores > 1} each fit runs single-threaded to avoid oversubscription;
when \code{cores == 1} each fit uses all cores.}

\item{M}{Top words for coherence/exclusivity.}

\item{seed}{RNG seed (held-out split + fits).}

\item{...}{Passed to \code{\link[=stm]{stm()}} (e.g. \code{max.em.its}, \code{init.type}).}
}
\value{
A \code{faSTM_searchk} object wrapping a tidy data.frame \code{results} with one
row per K (\code{K}, \code{heldout}, \code{semcoh}, \code{exclusivity}, \code{bound}).
}
\description{
Fits the model across a range of K and reports diagnostics for choosing it:
held-out likelihood (document completion), semantic coherence, exclusivity,
and the variational bound. Unlike \code{stm::searchK}, the per-K fits parallelize
across K (a long-standing request, bstewart/stm#262) and each fit is itself
fast (Rust), so a sweep that took minutes takes seconds.
}
