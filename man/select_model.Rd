% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select-model.R
\name{select_model}
\alias{select_model}
\title{Fit several models and keep the ones on the quality frontier}
\usage{
select_model(
  corpus,
  K,
  N = 10L,
  prevalence = NULL,
  content = NULL,
  init.type = "Random",
  cores = 1L,
  M = 10L,
  frexw = 0.7,
  seed = 1L,
  ...
)
}
\arguments{
\item{corpus}{A \code{faSTM_corpus}.}

\item{K}{Number of topics.}

\item{N}{Number of candidate models (distinct random inits).}

\item{prevalence, content}{Optional covariate formulas.}

\item{init.type}{Initialization; \code{"Random"} (the point of selecting) or
\code{"Spectral"} (deterministic — then all \code{N} are identical).}

\item{cores}{Candidates to fit in parallel.}

\item{M}{Top words for coherence/exclusivity scoring.}

\item{frexw}{Exclusivity FREX weight.}

\item{seed}{Base RNG seed (candidate i uses \code{seed + i - 1}).}

\item{...}{Passed to \code{\link[=stm]{stm()}}.}
}
\value{
A \code{faSTM_selectmodel}: \code{models} (the fits), \code{semcoh}, \code{exclusivity},
and \code{frontier} (indices of non-dominated models).
}
\description{
With random initialization the variational objective is multimodal, so the
standard workflow (cf. \code{stm::selectModel}) is to fit many models and keep
those on the semantic-coherence / exclusivity frontier, then choose among
them. faSTM fits the candidates in parallel.
}
