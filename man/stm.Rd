% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stm.R
\name{stm}
\alias{stm}
\title{Fit a structural topic model (fast Rust backend, stm-compatible object)}
\usage{
stm(
  documents,
  vocab,
  K,
  prevalence = NULL,
  content = NULL,
  data = NULL,
  max.em.its = 500L,
  emtol = 1e-05,
  init.type = c("Spectral", "Random", "LDA", "Custom"),
  init.beta = NULL,
  model = NULL,
  gamma.prior = c("Pooled", "L1"),
  gamma.l1.alpha = 0.001,
  sigma.prior = 0,
  seed = 1L,
  inference = c("batch", "svi"),
  batch_size = 256L,
  tau = 64,
  kappa = 0.7,
  num_threads = 0L,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{documents}{stm-format documents: a named list of \verb{2 x n_d} integer
matrices (row 1 = 1-based word id into \code{vocab}, row 2 = count). Produced by
\code{\link[stm:prepDocuments]{stm::prepDocuments()}}.}

\item{vocab}{Character vector of vocabulary terms.}

\item{K}{Number of topics.}

\item{prevalence}{A right-hand-side formula (e.g. \code{~ treatment + s(age)}) or a
design matrix; topic prevalence covariates. \code{data} supplies the variables.}

\item{content}{A right-hand-side formula naming a single categorical variable,
or a factor; the SAGE content covariate. \code{data} supplies the variable.}

\item{data}{A data.frame of document metadata (the \code{meta} from
\code{\link[stm:prepDocuments]{stm::prepDocuments()}}), aligned to \code{documents}.}

\item{max.em.its}{Maximum EM iterations (batch) / epochs (svi).}

\item{emtol}{Relative-bound convergence tolerance.}

\item{init.type}{Topic initialization: \code{"Spectral"} (stm's default),
\code{"Random"}, \code{"LDA"} (seed from a quick CVB0 LDA, like stm's collapsed-Gibbs
init), or \code{"Custom"} (seed from \code{init.beta} or a supplied \code{model}).}

\item{init.beta}{Optional K x V topic-word probability matrix to start the fit
from a given initialization (overrides \code{init.type}). Supplying R \code{stm}'s
exact spectral beta here reproduces that run — a guaranteed
"replicate the original" mode (topica #234/#235).}

\item{model}{A fitted model whose topic-word matrix seeds \code{init.type = "Custom"}.}

\item{gamma.prior}{Prevalence-coefficient prior: \code{"Pooled"} (ridge, stm
default) or \code{"L1"}.}

\item{sigma.prior}{Shrinkage applied to the topic covariance off-diagonal.}

\item{seed}{Integer seed (batch fit is reproducible from it).}

\item{inference}{\code{"batch"} (default, parity-validated) or \code{"svi"} (stochastic
variational; scales to large corpora — requires a topica build with STM-SVI).}

\item{batch_size, tau, kappa}{SVI controls (minibatch size; Robbins-Monro
\code{(tau + t)^(-kappa)} step schedule). Ignored when \code{inference = "batch"}.}

\item{num_threads}{Worker threads for the parallel variational E-step. \code{0}
(default) uses all cores; \verb{>= 1} pins a scoped pool. Results are identical
regardless of thread count.}

\item{verbose}{Logical; print progress.}
}
\value{
An object of class \code{c("faSTM", "STM")} — an stm-compatible fit.
}
\description{
A drop-in replacement for \code{\link[stm:stm]{stm::stm()}}'s fitting step. Accepts the same
\code{documents} / \code{vocab} / \code{prevalence} / \code{content} inputs, fits with topica's
Rust core, and returns an object compatible with the \code{stm} package so that
\code{\link[stm:labelTopics]{stm::labelTopics()}}, \code{\link[stm:plot.STM]{stm::plot.STM()}}, \code{\link[stm:findThoughts]{stm::findThoughts()}},
\code{\link[stm:sageLabels]{stm::sageLabels()}}, and \code{\link[stm:toLDAvis]{stm::toLDAvis()}} work unmodified. Use
\code{\link[=estimateEffect]{estimateEffect()}} from this package for the honest covariate effects.
}
